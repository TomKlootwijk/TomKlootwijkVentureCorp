# Examples

- `example_query.json`: one candidate in the authoritative G64/E32 profile.
- `example_output.json`: event produced by the Python oracle.
- `custom_glyph_encoder.txt`: explicit distinction between standard percentage arithmetic and the corpus-defined glyph transform.
